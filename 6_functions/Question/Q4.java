package Question;

public class Q4 {
    public static void main(String[] args) {
        int a = 2;
        int b = 5;
        int min = Math.min(a, b);
        Math.max(a, b);
        double sqrt = Math.sqrt(a);
        int abs = Math.abs(a);
        // int avg = Math.avg(a, b);
        double pow = Math.pow(a, b);
        System.out.println(min + min + sqrt + abs + pow);
    }

}
